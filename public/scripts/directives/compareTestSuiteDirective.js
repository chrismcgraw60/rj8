
juaApp.directive('compareTestSuite', ['$window', '$location', function($window, $location) {
	return {
        restrict: 'EA',
        transclude: true,
        templateUrl: 'assets/scripts/directives/compareTestSuiteTemplate.html',
        scope: true, // Prototypical parent scope inheritance.
        
        link: function(scope, element, attrs) {
        	
        	
        	
        }//link:
	}
}]);